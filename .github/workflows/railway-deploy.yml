name: Deploy API to Railway

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

env:
  NODE_VERSION: 20
  RAILWAY_PROJECT_ID: f7c96f7b-ed85-4ae9-9428-de3888942936
  RAILWAY_ENVIRONMENT: production
  RAILWAY_SERVICE: order-pulse-api

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install root deps
        run: npm ci

      - name: Lint frontend
        run: npm run lint

      - name: Test frontend (no coverage)
        run: npm test -- --coverage=false

      - name: Build frontend
        run: npm run build

      - name: Install server deps
        working-directory: server
        run: npm ci

      - name: Build server
        working-directory: server
        run: npm run build

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Railway CLI
        run: curl -fsSL https://railway.app/install.sh | sh

      - name: Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          NODE_VERSION: ${{ env.NODE_VERSION }}
          RAILWAY_PROJECT_ID: ${{ env.RAILWAY_PROJECT_ID }}
          RAILWAY_ENVIRONMENT: ${{ env.RAILWAY_ENVIRONMENT }}
          RAILWAY_SERVICE: ${{ env.RAILWAY_SERVICE }}
        working-directory: server
        run: |
          export PATH="$HOME/.railway/bin:$PATH"
          railway up --project "$RAILWAY_PROJECT_ID" --environment "$RAILWAY_ENVIRONMENT" --service "$RAILWAY_SERVICE"
